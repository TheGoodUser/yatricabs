import 'package:flutter/material.dart';

class AirportTransferCard extends StatelessWidget {
  const AirportTransferCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Airport Transfer Card'));
  }
}
