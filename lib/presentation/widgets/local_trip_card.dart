import 'package:flutter/material.dart';

class LocalTripCard extends StatelessWidget {
  const LocalTripCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Local trip'));
  }
}
